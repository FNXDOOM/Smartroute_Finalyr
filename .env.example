DATABASE_URL=postgresql://postgres:password@localhost:5432/smartrouteai

# Comma-separated list of allowed frontend origins, e.g. http://localhost:5173
ALLOWED_ORIGINS=http://localhost:5173

# Set to clerk after configuring Clerk. Keep Clerk credentials server-side.
AUTH_PROVIDER=clerk
CLERK_JWKS_URL=https://<your-clerk-domain>/.well-known/jwks.json
CLERK_ISSUER=https://<your-clerk-domain>
# Optional: set only if your Clerk token is configured with an audience claim.
CLERK_AUDIENCE=
# Optional: comma-separated browser origins allowed in the token's azp claim.
# Defaults to ALLOWED_ORIGINS when omitted.
CLERK_AUTHORIZED_PARTIES=http://localhost:5173
